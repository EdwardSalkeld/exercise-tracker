services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: exercise_tracker
      POSTGRES_USER: exercise_tracker
      POSTGRES_PASSWORD: exercise_tracker
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
